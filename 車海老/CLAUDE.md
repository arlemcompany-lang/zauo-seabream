# CLAUDE.md — 車海老（クルマエビ）説明ページ

## このフォルダについて
[[魚種別説明ページ]]の一つ。車海老（クルマエビ）の説明ページ。
親フォルダのCLAUDE.mdも参照: `../CLAUDE.md`

## 状態
**完成・公開済み**（2026-08-13）。8言語対応済み。
調理は **刺身◎・天婦羅〇・塩焼き〇・フライ◎・寿司〇（1貫 60円）** の5種類。
**残タスクは天婦羅・寿司の記号（暫定〇）の店舗確認。**
料理写真は**刺身の1枚を2026-09-04に掲載済み**（8ページで初）。他の調理法の写真は届き次第追加する。

### 料理写真（2026-09-04 掲載）
- `kuruma-ebi-sashimi.jpg` … 刺身。元は `~/Downloads/S__85803020.jpg`（1108×1477・410KB）。
  長辺900pxにリサイズし JPEG quality 82 で保存（675×900・134KB）。鯵ページの写真と同程度のサイズ感
- `.photo-placeholder`「写真準備中」を廃止し、鯵ページと同じ **`.photo-grid`** 構成に差し替えた。
  写真が1枚なので `grid-template-columns: 1fr`＋`figure` を `max-width:420px` で中央寄せ
  （縦長写真なので幅いっぱいに広げると間延びするため）。2枚目以降が届いたら `figure` を足す
- `figcaption` は `data-i18n="cap_sashimi"` で8言語対応。訳語はバッジの `b_sashimi` と揃えてある
- 不要になった `photo_soon` キーは8言語ぶん削除した

### 2026-09-04: 調理法カードを実メニューに合わせて修正
店舗の実メニュー写真 `調理方法１.jpg`（`~/Downloads`）と照合したところ3点ずれていた。
- **天婦羅を追加** … 実メニューでは「刺身・天婦羅」が1枚の写真枠に入っている
- **寿司〇（1貫 60円）を追加** … 実メニューに写真付きで「寿司 1貫 60円」
- **フライの「（追加料金あり）」を削除** … 「塩焼き・フライ」の写真枠に入っていて金額の記載が無い。
  車海老は通常780円→釣り価格680円で、金額が付いているのは寿司だけ。
  鯵ページ（2026-09-03）と同じ理由での削除
- これに伴い `fry_extra` を8言語から削除し、`b_tenpura` / `b_sushi` / `sushi_price` を8言語ぶん新設
- **天婦羅と寿司の記号〇は暫定**。おすすめ度は店舗に確認して調整する
素材と原稿は `車海老.md` に集約してある。

## 構成
- `index.html` — ページ本体（既存3ページと同じ構成。`../鮑/index.html` を雛形に作成）
- `kuruma-ebi.mp4` — トップの縦長動画（配置済み。元ファイル名 `車海老.mp4` からリネーム）
- 動画の背景は説明文と同じ淡色 `var(--bg)`（波柄背景は2026-08-21に廃止）

## 制作手順（既存3ページ踏襲）
1. 動画を `kuruma-ebi.mp4` としてこのフォルダに配置
2. `../伊勢海老/index.html` を複製し、名称・アクセントカラー・本文・`I18N` を差し替え
3. 豆知識は**必ずWebSearchで事実確認**してから掲載
4. 調理法カードのメニューと評価記号（◎〇△✕）は**店舗固有情報。必ずユーザーに確認**（推測で書かない）
5. Playwrightでスマホ表示を確認（`.video-wrap` は `calc(80vh - var(--langbar-h))`）
6. commit → push → 公開URLをユーザーに渡す（日本語部分はパーセントエンコード）

## 固定仕様（3ページ共通・変えない）
- `.video-wrap` の背景は `var(--bg)`＝本文と同じ淡色（2026-08-21に波柄＋`#b03052`から変更）
- 動画エリアの高さは `80vh` 基準（言語バーの高さを差し引く）
- 8言語対応（`ja`/`en`/`zh`/`ko`/`es`/`it`/`vi`/`tl`）。開いたときは**常に日本語**、選択は保存しない
- 国旗は**インラインSVG**（絵文字の国旗はWindowsのChrome/Edgeで描画されない）
- `.lang-name` のフォントは**ラテン系を先頭**（ベトナム語の「ế」が崩れるため）
- 名称の各言語表記は釣りゲーム `../../ざうおスマホゲーム/index.html` の `FISH_NAMES.ebi` に合わせる

## 追加料金・金額の表記ルール
金額は**実額で出す**（鯵ページで2026-09-03に新設した方式）。`sushi_price` はラテン文字圏が `¥60`、
中国語は「60日元」、韓国語は「60엔」。**平目2ヶ所・鮑1ヶ所はまだ `fry_extra`「（追加料金あり）」の
まま**なので、横展開するときは揃えること。

## アクセントカラー（案・未確定）
縞模様と加熱後の色から**オレンジ系**を想定。伊勢海老が赤系なので**区別がつく明るさにする**。
