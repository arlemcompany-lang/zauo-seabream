# CLAUDE.md — 鮑（アワビ）説明ページ

## このフォルダについて
[[魚種別説明ページ]]の一つ。鮑（アワビ）の説明ページ。
親フォルダのCLAUDE.mdも参照: `../CLAUDE.md`

## 状態
**未着手**（フォルダのみ作成済み・2026-08-13）。
素材と原稿は `鮑.md` に集約する。中身が埋まったら `index.html` を作る。

## 注意：この種は釣りゲームに登場しない
釣りゲーム（`../../ざうおスマホゲーム/index.html`）の `FISH_NAMES` に**アワビは無い**。
そのため8言語の名称は**新規に用意する**（既存3ページのように流用できない）。
表記を決めたら、ゲーム側に追加するかどうかもユーザーに確認する。

## 予定の構成
- `index.html` — ページ本体（既存3ページと同じ構成。雛形は `../鯛/index.html` を複製して作る）
- `awabi.mp4` — トップの縦長動画（**未配置**）
- 波柄背景は親フォルダの `../波柄_pattern.webp` を参照（新規に用意しない）

## 制作手順（既存3ページ踏襲）
1. 動画を `awabi.mp4` としてこのフォルダに配置
2. `../鯛/index.html` を複製し、名称・アクセントカラー・本文・`I18N` を差し替え
3. 豆知識は**必ずWebSearchで事実確認**してから掲載
4. 調理法カードのメニューと評価記号（◎〇△✕）は**店舗固有情報。必ずユーザーに確認**（推測で書かない）
5. Playwrightでスマホ表示を確認（`.video-wrap` は `calc(80vh - var(--langbar-h))`）
6. commit → push → 公開URLをユーザーに渡す（日本語部分はパーセントエンコード）

## 固定仕様（3ページ共通・変えない）
- `.video-wrap` の背景色 `#b03052` は**魚種で変えない**
- 動画エリアの高さは `80vh` 基準（言語バーの高さを差し引く）
- 8言語対応（`ja`/`en`/`zh`/`ko`/`es`/`it`/`vi`/`tl`）。開いたときは**常に日本語**、選択は保存しない
- 国旗は**インラインSVG**（絵文字の国旗はWindowsのChrome/Edgeで描画されない）
- `.lang-name` のフォントは**ラテン系を先頭**（ベトナム語の「ế」が崩れるため）

## アクセントカラー（案・未確定）
貝殻の内側（真珠層）から**青緑〜深緑系**を想定（既存: 鯛=ピンク／鯵=青／伊勢海老=赤）。
