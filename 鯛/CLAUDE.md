# CLAUDE.md — 鯛（タイ）説明ページ

## このフォルダについて
[[魚種別説明ページ]]の一つ。鯛（タイ）の説明ページ。
親フォルダのCLAUDE.mdも参照: `../CLAUDE.md`

## 状態
完成・公開済み。**2026-08-07に8言語対応済み**（鯵と同じ仕組み）。
公開URL: https://arlemcompany-lang.github.io/zauo-seabream/鯛/

**2026-09-04に調理法カードを店舗の実メニューに合わせて全面的に差し替えた**（鯵→縞鯵→鯛の順で横展開）。

## 構成
- `index.html` — ページ本体（動画トップ＋説明カード4枚：調理法／どんな魚？／どこにいる？旬はいつ？／豆知識）
- `tai.mp4` — トップの縦長動画

## 調理法カード（2026-09-04 更新）
根拠は店舗の実メニュー写真 `調理方法１.jpg` / `調理方法２.jpg`（`~/Downloads` にある一次資料）。
`調理方法２.jpg` は **タイ・ヒラメ・シマアジ 共通**の加工メニューシート。

現在のバッジ:
- 調理法 … 刺身◎／塩焼き〇／煮付け〇（追加料金 330円）／唐揚げ〇／フライ〇（追加料金 220円）／
  バターソテー〇（追加料金 220円）／漁師飯セット〇（追加料金 440円）
- 骨・アラを使って … みそ汁〇（220円）／かぶと煮〇（330円）

差し替え前は「焼き／煮付け／唐揚げ」＋「兜焼き／あら炊き／骨せんべい△／みそ汁」で、金額表示は無かった。
実メニューと突き合わせて次のように直した。

- `b_yaki`「焼き」→ `b_shioyaki`「塩焼き」（メニュー上の名称。加工料無料）
- 煮付け330円／フライ220円／バターソテー220円 を**実額表示**にした（鯵・縞鯵と同じ体系）
- **`骨せんべい` を削除**。`調理方法１.jpg` の「残ったアラでもう一品」によれば
  骨せんべい280円は**平目のみ**の対象（アジ骨せんべい110円はアジのみ）。タイは対象外
- 「兜焼き」「あら炊き」はメニューに無い名称。実メニューの **かぶと煮330円**（タイ・シマアジが対象）に統一
- みそ汁は「味噌汁 1杯につき220円」＝タイ・平目・シマアジ・伊勢海老が対象
- **漁師飯セット440円**は2026-09-04にクライアント指示で追加。メニュー上の正式名は
  「ざうおの漁師飯セット」（`調理方法２.jpg` の「鍋・ご飯」欄・**1人前**・おすすめ／店舗限定味）。
  バッジは尺の都合で「漁師飯セット」と短縮し、「1人前」は出していない。表記を変えるなら店舗確認のこと。
  金額キーは `ex440` を新設（他ページには無いキー）。
  **漁師飯セットは今のところ鯛ページのみに載せる**（2026-09-04 クライアント判断）。
  平目・シマアジも同じ加工メニューシートの対象だが、横展開の指示があるまで足さないこと

**記号（◎〇）は暫定**。おすすめ度は店舗判断なので実メニューからは決まらない。
唐揚げは実メニュー（`調理方法２.jpg`）に無いが、2026-09-04にクライアント指示で縞鯵・平目とともに掲載。
追加料金は付けていない（金額を出す場合は店舗に確認）。

### まだ載せていない実メニュー項目
天婦羅220円／寿司1貫60円／炙り刺し220円／胡麻醤油あえ220円／カルパッチョ330円／ユッケ330円／
にんにく醤油焼き110円／バジル焼き330円／明太マヨ焼き330円／酒蒸し330円／揚げ出し330円／
フィッシュ＆チップス420円／天丼480円／手巻き寿司セット580円／お茶漬けセット440円／
釜飯440円・雑炊480円・寄せ鍋530円・チゲ鍋530円（鍋は冬季限定）。増やすなら店舗確認のうえで。

### 追加料金の表記
金額ごとのキー `ex330` / `ex220` / `ex440`（加工の追加料金）と `p220` / `p330`（アラ料金）を8言語ぶん用意。
ラテン文字圏は `¥330`、中国語は「330日元」、韓国語は「330엔」。
`ex330` / `ex220` / `p220` / `p330` は縞鯵ページと完全に同じ訳文。`ex440` と `b_ryoshimeshi` は鯛ページが初出。

## 残タスク
- **調理法カードの評価記号（◎〇）の店舗確認**（品目と金額は実メニューどおり。おすすめ度だけ暫定）
- 調理法カードの料理写真が未設定（`.photo-placeholder`「写真準備中」のまま）。
  写真が届いたら鯵ページと同じ `.photo-grid` 構成に差し替える。
  差し替え時は `figcaption` に `data-i18n` を付け、`I18N` の8言語すべてにキャプションを足すこと

## 多言語の仕様（3ページ共通）
- **対応言語は釣りゲーム（`../../ざうおスマホゲーム/index.html`）と完全に同じ8言語・同じ並び順**：
  `ja` 日本語 / `en` English / `zh` 中文（簡体） / `ko` 한국어 / `es` Español / `it` Italiano / `vi` Tiếng Việt / `tl` Tagalog
  - 言語コードもゲームと同一。名称もゲームの `FISH_NAMES` に合わせている
- 動画の**上**に言語切替バー（`.langbar`）。`position: sticky` でスクロール中も追従
- 国旗は**インラインSVG**（絵文字の国旗はWindowsのChrome/Edgeで「JP」等の文字にしか描画されないため使わない）。配色はゲームのCanvas描画関数と揃えてある
- **開いたときは常に日本語**。選択は localStorage 等に保存しない（クライアント要件）
- 翻訳は `<script>` 内の `I18N` に集約。要素側は `data-i18n`（textContent）と `data-i18n-html`（innerHTML／`<strong>`や`<br>`を含む豆知識用）
- 評価記号（◎〇△✕）はHTML側に固定。翻訳対象はメニュー名と金額の表記のみ
- `.lang-name` のフォントは**ラテン系を先頭**にする。CJKフォントが先頭だとベトナム語の「ế」が合成表示になって崩れる
- 言語バーはスマホ4列×2段／640px以上8列×1段。その高さぶん `.video-wrap` を `calc(80vh - var(--langbar-h))` で縮める（スマホ111px／640px以上61px、Playwrightで実測）

## 表示確認（2026-09-04）
CDP（`Emulation.setDeviceMetricsOverride`）で390×844をエミュレートして実測。手順は `../縞鯵/CLAUDE.md` 参照。
ja/en/ko/es/vi の5言語で `scrollWidth` 390（横スクロールなし）／`.langbar` 111px ／
`.video-wrap` 564px（= 80vh − 111px、仕様どおり）。調理法カードの差し替え後も、
漁師飯セットの追加後も数値は変化なし。
バッジの折り返し・ベトナム語の声調記号とも崩れなし。
